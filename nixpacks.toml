[phases.setup]
# openssl required by Prisma linux-musl-openssl-3.0.x engine binary at runtime
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate"]

[start]
cmd = "node src/server.js"
